# Freebuff desktop app local data (never commit)
.freebuff/

# Vercel CLI local state
.vercel/

# Local environment files (never commit real values) — but keep the templates
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example
